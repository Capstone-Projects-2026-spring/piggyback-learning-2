FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Copy the rest of the source code
COPY . .

# Expose the Next.js dev port
EXPOSE 3000

# Next.js 16 + Turbopack works beautifully on Linux (Docker)
CMD ["npm", "run", "dev"]